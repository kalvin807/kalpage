---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<section class="relative z-1 py-8">
  <header class="mb-6">
    <h2 class="m-0 font-mono text-xs tracking-[0.08em] text-accent">{title}</h2>
  </header>
  <slot />
</section>
