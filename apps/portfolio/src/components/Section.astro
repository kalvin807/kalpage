---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<section class="border-t border-accent-10 py-8 sm:py-10">
  <header class="mb-6">
    <h2 class="m-0 font-mono text-sm text-text-muted">{title}</h2>
  </header>
  <slot />
</section>
