---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<section class="relative z-1 py-8">
  <h2 class="font-heading text-[0.8rem] font-medium uppercase tracking-[0.15em] text-accent mb-8">{title}</h2>
  <slot />
</section>
